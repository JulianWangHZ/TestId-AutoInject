node_modules/
dist/
testid-map.json
*.log
.DS_Store

# Rust / SWC plugin build artifacts (Cargo.lock is committed to pin versions)
packages/*/target/

# Playwright MCP scratch (snapshots, console logs)
.playwright-mcp/
